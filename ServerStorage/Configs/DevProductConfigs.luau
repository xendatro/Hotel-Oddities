local DevProductConfigs = {
	-- [ProductId] = function(player, ...)
	--     -- Handle the product purchase
	-- end
	
	-- Example:
	-- [123456789] = function(player, amount)
	--     print(player.Name .. " purchased product with amount: " .. amount)
	-- end,
}

return DevProductConfigs
