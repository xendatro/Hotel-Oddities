return {
	SendInterval = 0.1,
	SendThreshold = math.rad(2),

	MaxPitch = math.rad(55),
	MaxYaw = math.rad(40),

	Neck = { Pitch = 0.6, Yaw = 0.65 },
	Waist = { Pitch = 0.35, Yaw = 0.15 },

	Smoothing = 8,

	MimicUpdateInterval = 0.1,

	Debug = {
		IncludeLocalPlayer = false,
	},
}
