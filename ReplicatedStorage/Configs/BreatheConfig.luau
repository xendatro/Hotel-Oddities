return {
	Period = 4.2,

	--- Portion of the cycle spent inhaling; the rest is a slower exhale.
	InhaleFraction = 0.42,

	Waist = { Pitch = math.rad(1.8) },
	Neck = { Pitch = math.rad(1.1) },
	Shoulder = { Roll = math.rad(1.2) },
	Root = { Rise = 0.04 },

	Smoothing = 12,
	MaxDistance = 90,
}
